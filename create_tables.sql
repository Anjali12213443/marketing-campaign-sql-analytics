CREATE TABLE marketing_campaigns (
    campaign_id INTEGER,
    campaign_type TEXT,
    customer_id INTEGER,
    impressions INTEGER,
    clicks INTEGER,
    conversions INTEGER,
    cost REAL,
    revenue REAL,
    start_date DATE,
    end_date DATE
);